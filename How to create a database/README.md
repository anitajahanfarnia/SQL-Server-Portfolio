Institute Database:
This project focuses on creating a relational database using SQL Server. The database and its tables are created using SQL queries, and the required data is inserted into the tables.
The project includes dimension tables, a fact table, primary and foreign keys, and relationships between the tables. A database diagram is also created to visualize the database structure and relationships.

Tools:
- SQL Server Management Studio (SSMS)
- T-SQL
